# Настройка базы данных

Для работы мессенджера требуется PostgreSQL.

1. Создайте базу данных с названием `messenger_db`.
2. Выполните скрипт `schema.sql` для создания таблиц.
3. Убедитесь, что логин/пароль в файле `database.py` совпадают с вашими настройками (по умолчанию `postgres` / `12345`).